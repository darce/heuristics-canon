# Security Heuristics Lexicon

## About this document

Decision rules for security as a spanning concern: the trust boundaries,
injection sinks, privilege grants, and misconfigurations that turn a working
feature into an exploitable one. Referenced, not read end to end: a threat
model, diff, or audit cites a rule by ID where the decision happens, and this
file holds the claim behind it. Like accessibility, security cuts across the
other lexicons (an SQL-injection rule is also a data rule, a least-privilege
rule is also an architecture rule), so it earns its own lexicon rather than
scattering.

Scope: AI/ML and agent security, general web application security,
secure-design principles, and the PHP / WordPress / PostgreSQL stack. It does
not yet cover cryptography, network security, OS hardening, or identity
protocols in depth; those await both the sources and the pass.

**Reading a row**

| Column | What it holds |
|---|---|
| ID | stable citation key, immutable once assigned; cite it as `[WEB-01]` |
| Trigger | observable in a diff, config, architecture plan, or audit finding |
| Rule | the falsifiable claim: condition, action, consequence |
| Answers | the one question to ask before the decision is made |
| T·P | tier and phase, below |
| Src | source slug, resolved in the public repo's `SOURCES.md`, or `bootstrap` for a not-yet-sourced row; the distillation behind a slug stays in the private research corpus |

Tier: **B**locker (exploitable, data-loss, or auth-bypass if violated),
**S**hould (strong default), **J**udgment. Phase: **p**lan (architecture and
threat model), **w**rite (code), **r**eview (config and audit).

Provenance, grounding in progress. §1 (AI/ML and agent security) is sourced to
Steve Wilson, *The Developer's Playbook for Large Language Model Security*
(`llm-security-playbook`), extended to adversarial ML (poisoning, backdoors,
evasion, ML supply chain) by Sotiropoulos, *Adversarial AI*
(`sotiropoulos-adversarial-ai`). §2 (web application security) has been
re-grounded against the canonical web-security texts: the browser-model rules
(XSS, CSRF, cookies, CSP/HSTS, redirects, TLS, clickjacking, CORS, content
sniffing, `postMessage`) to Michal Zalewski, *The Tangled Web*
(`zalewski-tangled-web`), and the server-side rules (SQL/OS/LDAP/XXE injection,
SSRF, path traversal, broken access control, sessions, authentication) to
Stuttard & Pinto, *The Web Application Hacker's Handbook* (`stuttard-wahh`). A
few WEB rows those two texts do not establish (mass assignment [[WEB-19]](security.md#web-19), native
deserialization [[WEB-20]](security.md#web-20), upload hardening [[WEB-12]](security.md#web-12), secrets-in-VCS [[WEB-16]](security.md#web-16))
remain `bootstrap`. Several §3 (PHP) rows are grounded in *Modern PHP Security*
(Chauchefoin & David, sec4dev 2020). §§4–5 (WordPress, PostgreSQL) remain a
`bootstrap` pass: the corpus holds no WordPress- or PostgreSQL-specific security
text, so those rows are drawn from established practice and held at tier S/J,
not settled Blockers-by-citation. §6 (secure design principles: least privilege,
complete mediation, minimize the trusted computing base, fail-safe defaults,
defense in depth, threat modelling) is grounded in Anderson, *Security
Engineering* (`anderson-security-engineering`). Treat any `bootstrap` row as a
strong default under review. Cross-lexicon borders cite `↔ eng/biz` rules
instead of restating them.

## 1. AI/ML & Agent Security

> `SEC-01..10` (LLM/agent) are sourced to Wilson, *The Developer's Playbook for Large Language Model Security* (`llm-security-playbook`; moved here from engineering; existing `[SEC-02]` citations still resolve). `SEC-11..16` extend the family to **adversarial ML** (training-data poisoning, backdoors/trojans, evasion, and the ML supply chain) from Sotiropoulos, *Adversarial AI* (`sotiropoulos-adversarial-ai`).

| ID | Trigger | Rule | Answers | T·P | Src |
| --- | --- | --- | --- | --- | --- |
| SEC-01<a name="sec-01"></a> | Data crossing any component boundary (user↔app, app↔LLM, LLM↔services) | **Validate at every trust boundary** — trust changes at each crossing; the LLM's output side is a boundary too (↔ [[WEB-08]](security.md#web-08) object access the route check does not enforce; ↔ [[WEB-21]](security.md#web-21) a frame that delivers clicks the eye never chose; ↔ [[PG-02]](security.md#pg-02) tenant isolation left to every handler's WHERE; ↔ grph [[GRPH-18]](graph-theory.md#grph-18) a borderline match that asserts identity; ↔ ux [[PERC-03]](interaction-ux.md#perc-03) a group the layout implies but the code does not wire; ↔ ux [[HAI-05]](interaction-ux.md#hai-05) autonomy named beyond the tested envelope) | Where does trust change, and what validation runs there — in and out? | S·p | llm-security-playbook ch-3 |
| SEC-02<a name="sec-02"></a> | LLM output passed to SQL, shell, eval, or rendered as HTML | **Treat LLM output as untrusted input** — generated text can carry injected payloads into any interpreter (↔ [[WEB-01]](security.md#web-01), [[WEB-04]](security.md#web-04) generated text is untrusted input at every interpreter sink) | Is the output parameterized, escaped, or encoded before the sink? | B·r | llm-security-playbook ch-7 |
| SEC-03<a name="sec-03"></a> | User or external content interpolated into an LLM prompt | **Delimit untrusted prompt segments** — the model can't distinguish data from instructions unless the boundary is marked | Is every untrusted segment structurally marked and out of instruction position? | S·w | llm-security-playbook ch-4 |
| SEC-04<a name="sec-04"></a> | LLM granted a tool, plugin, API scope, or DB permission | **Least-privilege agency** — a jailbroken model exercises every permission it holds (↔ [[PG-01]](security.md#pg-01); ↔ biz [[BOOT-07]](business-marketing.md#boot-07) one landlord's hold is fatal when there is no second exit) | Is this the minimum scope the feature needs? | B·p | llm-security-playbook ch-7 |
| SEC-05<a name="sec-05"></a> | LLM can trigger an irreversible, financial, or destructive action | **Human-in-the-loop gate** — autonomy plus prompt injection equals attacker-driven transactions (↔ [[SEC-04]](security.md#sec-04) broad scope is the irreversible side of the same trade; ↔ biz [[CLM-02]](business-marketing.md#clm-02) the aggressive phrasing is the underpriced irreversible claim; ↔ eng [[RLSE-08]](engineering.md#rlse-08) write the rollback before ship, not at 2am) | Does a human approve before the side effect executes? | B·p | llm-security-playbook ch-7 |
| SEC-06<a name="sec-06"></a> | Secrets or PII in a prompt template, fine-tune set, or RAG store | **Don't teach the model what it must never say** — anything the LLM can read can be extracted by any user (↔ [[WEB-16]](security.md#web-16) a secret the model was taught is a secret the user can extract) | What happens if this datum is disclosed verbatim? | B·p | llm-security-playbook ch-5 |
| SEC-07<a name="sec-07"></a> | Web pages, files, or DB rows fed into a RAG prompt | **Retrieved content is attacker content** — indirect injection rides in on documents | Is retrieved text screened and confined to a data role? | S·w | llm-security-playbook ch-4 |
| SEC-08<a name="sec-08"></a> | Public LLM endpoint or feature without usage limits | **Rate-limit and cap per caller** — unmetered access invites DoS and denial-of-wallet (↔ [[WEB-17]](security.md#web-17) unmetered public endpoints are DoS and denial-of-wallet by default) | Are per-caller limits, token caps, and budget alerts configured? | S·w | llm-security-playbook ch-8 |
| SEC-09<a name="sec-09"></a> | LLM-suggested package added to a dependency manifest | **Verify hallucinated dependencies** — attackers register malware under names models invent (↔ eng [[AGT-02]](engineering.md#agt-02) a name that was never resolved is not a referent) | Does this package exist with the expected owner and real history? | B·r | llm-security-playbook ch-6 |
| SEC-10<a name="sec-10"></a> | Model or dataset pulled from a hub at build/run time | **Pin and record provenance** — unpinned weights are an unauditable dependency (↔ [[SEC-09]](security.md#sec-09) a model-invented package name is not a referent until it resolves; ↔ grph [[GRPH-14]](graph-theory.md#grph-14) every artifact must point at the inputs that produced it; ↔ writ [[WRIT-26]](writing.md#writ-26) name the source or cut the claim; ↔ epi [[FORE-06]](epistemics.md#fore-06) a point probability with no decomposition has no walkable ancestors; ↔ a11y [[A11Y-27]](accessibility.md#a11y-27) authority is the primary text, not a derived summary; ↔ writ [[WRIT-41]](writing.md#writ-41) a citation that cannot be followed is worse than none) | Is the exact revision pinned and integrity verified? | S·w | llm-security-playbook ch-9 |
| SEC-11<a name="sec-11"></a> | A training or fine-tune job reads data from the web, users, vendors, or a continuous live feed | **Poison-gate training data** — data crossing a trust boundary into training needs lineage and versioning, statistical anomaly gates, *and* model-aware poison probes (activation clustering / spectral signatures / **RONI**), because **data poisoning** — including **clean-label** poison that passes label QA and slips statistical outlier checks — rewrites model integrity before any inference defense applies (↔ [[MLDATA-04]](ml-systems.md#mldata-04) data crossing a trust boundary into training needs lineage before the model absorbs it) | Who can write rows this model will learn from, and does the gate run model-aware poison probes, not just label QA? | B·p | sotiropoulos-adversarial-ai |
| SEC-12<a name="sec-12"></a> | Accepting third-party weights, or promoting a model to the registry | **Probe for backdoors before promote** — run trigger/canary and activation/spectral probes (and **RONI** where the training set's influence is still available) on accepted weights, because clean test accuracy does not reveal a **backdoor/trojan** that flips class under a fixed patch (↔ eng [[TEST-15]](engineering.md#test-15) a green suite that never exercised the failure certifies nothing; ↔ ml [[COST-04]](ml-systems.md#cost-04) utilization green while accepted output is not; ↔ biz [[OPS-01]](business-marketing.md#ops-01) the metric pays for the 2am behavior, not the one you meant; ↔ biz [[AIPX-02]](business-marketing.md#aipx-02) offline score up while acceptance stays flat) | Would this model misclassify under a fixed trigger while passing the standard test set? | B·r | sotiropoulos-adversarial-ai |
| SEC-13<a name="sec-13"></a> | An inference loader deserializes model files (pickle / custom ops / `Lambda` layers) | **Model files are code** — load weights only from a signed, allowlisted source in a non-executable format (e.g. safetensors), scan for non-allowlisted layers, and verify the architecture against an approved card, because a **trojan** payload needs no poisoned training set (↔ [[WEB-20]](security.md#web-20) deserializing untrusted model bytes is the same RCE class as untrusted objects) | Can loading this artifact execute code or branch on a hidden trigger? | B·w | sotiropoulos-adversarial-ai |
| SEC-14<a name="sec-14"></a> | A classifier is exposed on a public or attacker-reachable inference API (vision / NLP / audio) | **Evasion budget in the eval gate** — put a documented **adversarial-example** eval (FGSM/PGD-class; patches / text as the domain demands) with a max attack-success rate in CI, and add adversarial training or layered defenses when the harm is high (safety, fraud, auth, moderation), because clean metrics hide inference-time **evasion** (≠ [[EVAL-06]](ml-systems.md#eval-06), which measures production noise, not an adaptive adversary) | What attack suite and acceptable attack-success rate are in CI for this threat model? | S·p | sotiropoulos-adversarial-ai |
| SEC-15<a name="sec-15"></a> | Train or serve resolves packages, base images, or models from a public `pip` / hub / `from_pretrained` | **Pin the ML supply chain** — use allowlisted private indexes, digest-pinned images, and an AI/ML SBOM, and re-eval + poison-detect any transfer-learning checkpoint, because a poisoned package or upstream checkpoint bypasses in-house data QA; pin the model/dataset revision itself under [[SEC-10]](security.md#sec-10) (↔ [[EMB-05]](ml-systems.md#emb-05) an unpinned package, image, or checkpoint is a silent supply-chain rewrite) | Which index, image, and checkpoint are pinned, and is the transfer-learning base re-evaluated? | B·w | sotiropoulos-adversarial-ai |
| SEC-16<a name="sec-16"></a> | Identities can mutate training data, the feature store, or the model registry | **Least privilege on the ML control plane** — split train / promote / serve rights and monitor writes to anything that becomes production truth, because a classic **least-privilege** failure enables poison and trojan with no novel math (↔ [[SEC-04]](security.md#sec-04); [Principle 14] both shrink what a single compromised identity can reach) | Which human or job can silently replace the production weights or 1% of the labels? | B·p | sotiropoulos-adversarial-ai |
| SEC-17<a name="sec-17"></a> | A capture-facing identity system reports only identity match rates (true-accept, false-match) with no attack-presentation error table | **Attack rates are not match rates** — when live captures drive identity decisions, report presentation-attack detection error separately from identity error — artefacts accepted as genuine, and genuine presentations rejected — at a declared detection threshold and per named attack class, because a true-accept rate is measured against cooperative subjects and says nothing about how often a print, replay, or mask is accepted (↔ [[SEC-14]](security.md#sec-14) the same clean-metric blindness, digital perturbation rather than physical presentation) | Are attack-presentation error rates reported separately from identity false-match rates, at a declared detection threshold? | S·r | handbook-face-recognition |
| SEC-18<a name="sec-18"></a> | An accept fires on similarity score while the liveness check runs after the accept, is logged only, or is fused with no written policy | **The liveness verdict must gate the accept** — when a live capture can be a print, replay, or worn mask, the presentation-attack stage must reject before the gallery comparison commits an accept; if the two scores are fused instead, the fusion policy and both thresholds must be written down and testable, because an attacker holding an enrolled person's photograph scores as that person, and a liveness signal nothing blocks on is telemetry, not a control | Can a presented photograph or replayed video of an enrolled person reach a final accept on similarity alone? | B·w | handbook-face-recognition |
| SEC-19<a name="sec-19"></a> | Liveness is an interactive motion challenge (blink, smile, turn head) and the attack evaluation set holds only flat printed photographs | **A motion challenge is not an attack set** — when liveness rests on interactive motion, extend the evaluation set to replayed video and to partial or three-dimensional artefacts (cut-out regions, worn masks, attack eyewear) before the ship gate passes, because a recording of the enrolled person performing the requested action satisfies a motion challenge, and a region-specific check is defeated by an artefact covering only that region | Does the attack set include replayed video and partial or three-dimensional artefacts, or only static print? | S·p | handbook-face-recognition |
| SEC-20<a name="sec-20"></a> | Presentation-attack detection is certified on the same camera, lighting, and capture protocol it was trained on, while the product ships to varied client devices | **Certify liveness on held-out capture hardware** — when client sensors, resolution, or lighting differ from the training capture, report detection error under a leave-one-device or leave-one-capture-source protocol, because cross-source error degrades sharply under illumination, resolution, and sensor-noise shift, so a matched-domain number certifies the lab and the first unseen camera is a field bypass rather than a regression (↔ ml [[FAIR-05]](ml-systems.md#fair-05) capture conditions are evaluation factors — this is the held-out form of that factor) | Is there a leave-one-device table, or only same-camera lab numbers? | S·r | handbook-face-recognition |

## 2. Web Application Security

> Re-grounded against Zalewski, *The Tangled Web* (`zalewski-tangled-web`, browser security model) and Stuttard & Pinto, *The Web Application Hacker's Handbook* (`stuttard-wahh`, server-side attacks). The four rows those texts do not establish ([[WEB-12]](security.md#web-12), [[WEB-16]](security.md#web-16), [[WEB-19]](security.md#web-19), [[WEB-20]](security.md#web-20)) remain `bootstrap`.

| ID | Trigger | Rule | Answers | T·P | Src |
| --- | --- | --- | --- | --- | --- |
| WEB-01<a name="web-01"></a> | User-controlled data concatenated into an SQL string (or interpolated into a query without a binder) | **Parameterize every query** — string-built SQL lets any quote/terminator break out into code; use prepared statements + bound params, including for values re-read from the database (second-order injection); binders bind values, so dynamic identifiers need [[WEB-35]](security.md#web-35) (↔ eng [[DATA-12]](engineering.md#data-12) both fail when a value is interpolated into a query instead of bound) | Is every SQL value bound — including stored values re-used in a later query? | B·w | stuttard-wahh |
| WEB-02<a name="web-02"></a> | User input passed to `exec`/`system`/`shell_exec`/`Runtime.exec`/`ProcessBuilder` without an argv array | **Never shell-interpolate untrusted input** — metacharacters become a second command; use argv APIs or refuse shell (↔ eng [[DATA-12]](engineering.md#data-12) never interpolate untrusted bytes into an interpreter) | Is the OS command built as discrete args with no shell? | B·w | stuttard-wahh |
| WEB-03<a name="web-03"></a> | LDAP filter built with string concat of request fields | **Allowlist LDAP filter input** — `)(uid=*` style input widens the filter and bypasses authz; permit only simple allowlisted characters and reject the filter metacharacters — parentheses, `*`, `&`, `=`, pipe — and null bytes, or use a typed directory API (WAHH's stance is reject, not sanitize-in-place) (↔ eng [[DATA-12]](engineering.md#data-12) metacharacter breakout from data into control plane) | Is every LDAP filter value allowlisted and metacharacter-rejected? | B·w | stuttard-wahh |
| WEB-04<a name="web-04"></a> | Response body echoes request data (or stored user content) into HTML without context-aware encoding | **Encode for the sink context** — unescaped HTML lets scripts run as the victim (reflected/stored XSS) | Is every untrusted string escaped for HTML/attr/JS/URL before emit? | B·w | zalewski-tangled-web |
| WEB-05<a name="web-05"></a> | Client-side code writes `location`/`document.write`/`innerHTML` from `location.hash`/`postMessage`/URL params | **Treat DOM sinks as XSS sinks** — client-only flows still execute attacker script in the origin | Is untrusted data never assigned to HTML/JS sinks without sanitization? | B·w | zalewski-tangled-web |
| WEB-06<a name="web-06"></a> | State-changing POST/PUT/DELETE lacks a per-session CSRF token verified server-side (`SameSite` cookies are defense-in-depth, not a substitute) | **Require anti-CSRF on mutations** — a third-party site can forge the browser's cookies into a state change | Does every state change verify a CSRF token or equivalent origin binding? | B·w | zalewski-tangled-web |
| WEB-07<a name="web-07"></a> | Server fetches a URL/host derived from user input (webhooks, previews, importers) | **Block SSRF to internal nets** — attacker-chosen URLs reach link-local/metadata/admin services; allowlist destinations or deny private ranges after re-resolve, and treat the fetched response body as untrusted (never reflect it raw into HTML/JSON) | Is the fetch target allowlisted or proven non-internal, and the returned body untrusted? | B·w | stuttard-wahh |
| WEB-08<a name="web-08"></a> | Object ID / path param used in a query without comparing ownership or tenant to the authenticated principal | **Authorize the object, not just the route** — knowing another ID is enough for IDOR/data disclosure (↔ eng [[ARCH-13]](engineering.md#arch-13) a property left to handler vigilance is not enforced; ↔ biz [[CLM-03]](business-marketing.md#clm-03) a policy sentence the implementation does not hold; ↔ ml [[PROV-05]](ml-systems.md#prov-05) a prediction an undeclared consumer will treat as a decision; ↔ eng [[TEAM-01]](engineering.md#team-01) an architectural split the org does not communicate across; ↔ eng [[NAME-03]](engineering.md#name-03) a name that promises a type or behavior the code does not hold) | Does the handler enforce ownership/tenant before read or write? | B·w | stuttard-wahh |
| WEB-09<a name="web-09"></a> | Session ID accepted from URL, or session not regenerated on privilege change (login) | **Rotate session ID on auth** — fixation reuses a pre-set SID after login as the victim | Is a new session ID issued at login and the SID never taken from the URL? | B·w | stuttard-wahh |
| WEB-10<a name="web-10"></a> | Auth cookie set without `Secure`+`HttpOnly` on a TLS site | **Lock down cookie flags** — set `Secure`+`HttpOnly` and a tight domain/path scope (Zalewski ch.9); missing flags enable theft via XSS or leakage over HTTP. `SameSite` is a later addition (defense-in-depth, not from this source) | Are session cookies `Secure` and `HttpOnly` (and `SameSite` where supported)? | S·r | zalewski-tangled-web |
| WEB-11<a name="web-11"></a> | Password login for privileged accounts with no second factor or step-up | **Multi-factor / step-up for privileged auth** — require a second factor and step-up re-authentication for admin and high-risk paths; password-only admin falls to phishing and reuse, and a token alone won't stop a live attacker (↔ [[WEB-34]](security.md#web-34) step-up per action) | Is a second factor or step-up reauth required for admin and high-risk actions? | S·p | stuttard-wahh |
| WEB-12<a name="web-12"></a> | Upload accepts arbitrary extension/content-type and stores under a web-served path | **Store uploads outside exec root** — executable or HTML uploads become RCE or stored XSS | Is the upload non-executable, type-validated, and outside the docroot? | B·w | owasp-web-hardening §file-upload |
| WEB-13<a name="web-13"></a> | User path segment joined to a base directory without canonicalization + prefix check | **Reject path traversal** — `../` escapes the intended root and reads secrets | Is the resolved path verified to stay under the allowed base? | B·w | stuttard-wahh |
| WEB-14<a name="web-14"></a> | Redirect target (`?next=`, `return_url`) taken from the request without a same-origin/allowlist check | **Allowlist redirect targets** — off-site redirects turn the app into a phishing trampoline; require same-site or an explicit allowlist and reject non-`http(s)` schemes (`data:`/`javascript:` inherit the origin) | Is the redirect target same-site or allowlisted, and non-pseudo-URL? | S·w | zalewski-tangled-web |
| WEB-15<a name="web-15"></a> | App responses omit CSP and HSTS (or set weak/default headers) on HTTPS endpoints | **Ship CSP + HSTS** — missing headers leave XSS impact high and enable SSL strip | Are CSP and HSTS present and non-permissive on all HTTPS responses? | S·r | zalewski-tangled-web |
| WEB-16<a name="web-16"></a> | API keys, passwords, or private keys appear in source, committed config, or client bundles | **Never commit secrets** — repo/history exposure is permanent credential theft (↔ [[SEC-06]](security.md#sec-06) a secret in repo or prompt is extractable by anyone who can read the channel) | Are secrets only in a secret store or env, never in VCS or client code? | B·r | owasp-web-hardening §secrets |
| WEB-17<a name="web-17"></a> | Auth, password-reset, or expensive endpoints have no per-IP/account rate limit | **Rate-limit abuse endpoints** — unbounded attempts enable credential stuffing and DoS (↔ [[SEC-08]](security.md#sec-08) unbounded auth and expensive routes are the same abuse surface as unmetered LLM calls) | Are login/reset/OTP routes rate-limited and lockout-aware? | S·w | stuttard-wahh |
| WEB-18<a name="web-18"></a> | HTTPS page loads active `http:` subresources, or admin/login served over cleartext | **No mixed content; ship HSTS** — load only HTTPS active subresources, send `HSTS`, set `Secure` cookies, and allow no cleartext admin, or active MITM replaces script and owns the origin (a TLS-version/cipher floor and HSTS preload are modern additions beyond this source) | Any `http:` active subresource or cleartext admin, and is HSTS set? | B·r | zalewski-tangled-web |
| WEB-19<a name="web-19"></a> | Framework mass-assigns request body fields onto a model/entity | **Allowlist assignable model fields** — bind only an explicit safe field list when mapping a request body onto an entity; an extra key like `role` writes a protected attribute (mass assignment) (authz *flags* are [[WEB-32]](security.md#web-32); WordPress is [[WP-14]](security.md#wp-14)) | Is the binder restricted to an explicit safe field list? | B·w | owasp-web-hardening §mass-assignment |
| WEB-20<a name="web-20"></a> | `unserialize`/`pickle`/`ObjectInputStream`/binary object decode on untrusted bytes | **Refuse untrusted deserialization** — gadget chains turn bytes into RCE (↔ [[PHP-02]](security.md#php-02) native object decode of untrusted bytes is RCE via gadget chains) | Is untrusted input never deserialized with a native object format? | B·w | owasp-web-hardening §deserialization |
| WEB-21<a name="web-21"></a> | A state-changing or authenticated UI can be framed cross-origin (no frame-denial) | **Deny cross-origin framing** — send `X-Frame-Options: DENY`/`SAMEORIGIN` or CSP `frame-ancestors` on sensitive UI, or an opaque overlay delivers the victim's clicks with ambient cookies (**clickjacking / UI redress**) (↔ [[WEB-08]](security.md#web-08) ambient authority past an unenforced object boundary) | Can an attacker's page iframe this action UI? | S·r | zalewski-tangled-web |
| WEB-22<a name="web-22"></a> | User-controlled bytes served as `text`/`json`/`csv`/unknown type without `nosniff` | **Stop content sniffing** — set the correct `Content-Type` plus `X-Content-Type-Options: nosniff`, and prefer `Content-Disposition: attachment` for downloads, or a browser may render the payload as HTML and run script | Could a browser sniff this response as HTML? | S·w | zalewski-tangled-web |
| WEB-23<a name="web-23"></a> | `Access-Control-Allow-Origin: *` or a reflected `Origin` on a credentialed/private API | **Allowlist CORS origins** — echo only exact allowlisted origins and never pair `*` with credentials, or CORS hands attacker pages the responses the same-origin policy would have hidden | Can another origin read this body with the user's cookies? | S·r | zalewski-tangled-web |
| WEB-24<a name="web-24"></a> | A `postMessage` handler checks `event.origin` with substring/`endsWith`, or not at all | **Verify message origin exactly** — compare `event.origin` with `===` against an allowlist, or a fuzzy match accepts `trusted.com.attacker.tld` (↔ grph [[GRPH-18]](graph-theory.md#grph-18) a borderline match must not assert identity) | Is the message origin compared exactly to a fixed list? | S·w | zalewski-tangled-web |
| WEB-25<a name="web-25"></a> | Request data flows into a response header (`Location`, `Set-Cookie`, custom) | **Strip CR/LF from header values** — never build a header by concatenating untrusted input; newline injection yields header splitting, XSS, and cache poisoning | Is any response-header value built from request data? | B·w | zalewski-tangled-web |
| WEB-26<a name="web-26"></a> | An XML parser processes untrusted XML with DTD / external-entity resolution enabled | **Disable XML external entities** — turn off DTD and external-entity resolution on untrusted XML, or `SYSTEM` entities read local files and pivot to SSRF (**XXE**; ≠ [[WEB-20]](security.md#web-20) — XML entity resolution, not native object decode) | Are DTDs and external entities disabled on every untrusted XML parser? | B·w | stuttard-wahh |
| WEB-27<a name="web-27"></a> | The server builds a back-end request or query from raw user strings (or duplicate params) | **Delimit back-end parameters** — encode and allowlist the parameter set for a server-built request; a duplicate or injected key overrides the intended value (**HTTP parameter pollution**) | Can a user inject `&role=admin` or a duplicate key into a back-end call? | S·w | stuttard-wahh |
| WEB-28<a name="web-28"></a> | Login / register / reset responses differ by whether the username exists (status/body/timing) | **Uniform auth responses** — make valid- and invalid-username responses identical in content and timing, or the endpoint leaks a target list (**username enumeration**) | Do real- and fake-user auth responses match aside from nonces? | S·r | stuttard-wahh |
| WEB-29<a name="web-29"></a> | Session identifiers are sequential, structured, or otherwise low-entropy | **Unpredictable session tokens** — generate high-entropy, meaningless session IDs, or predictable tokens let an attacker mint another user's session | Given a sample of session IDs, can the next be forecast or mapped to a user? | B·w | stuttard-wahh |
| WEB-30<a name="web-30"></a> | Logout only clears the client cookie; the server session stays valid; no idle expiry | **Terminate sessions server-side** — invalidate server session state on logout and idle timeout, not just the client cookie, or a hijacked SID stays live after "logout" | After logout, does replaying the old SID still authorize? | B·w | stuttard-wahh |
| WEB-31<a name="web-31"></a> | A privileged URL or method works for a low-privilege or anonymous caller (the UI merely hides it) | **Enforce authorization on every function** — check the required role on the server for each URL and method; hiding a link or trusting a client `isAdmin` flag is not access control (**vertical privilege escalation**; ↔ [[WP-03]](security.md#wp-03) for the WordPress capability check) | Can a normal session reach an admin URL or a method only referenced in client JS? | B·w | stuttard-wahh |
| WEB-32<a name="web-32"></a> | An access decision reads a role/level from a request parameter, hidden field, or cookie (`admin=true`) | **Never authorize from client-supplied flags** — load authority from server-side session state only; request parameters are attacker-controlled (**parameter-based access control**; ↔ [[WEB-19]](security.md#web-19) mass-assigned model fields, [[WP-14]](security.md#wp-14) WordPress role fields) | Does flipping `admin=true` or a role cookie change what the user can do? | B·w | stuttard-wahh |
| WEB-33<a name="web-33"></a> | Authorization checks are copy-pasted per handler; some paths are unchecked | **Deny-by-default authorization on every path** — enforce access control through shared, session-driven, deny-by-default checks covering every sensitive handler (one module or equivalent middleware), or piecemeal per-handler checks miss paths and rot under change | Is there any code path that serves sensitive data without the shared deny-by-default authz check? | S·p | stuttard-wahh |
| WEB-34<a name="web-34"></a> | A high-impact action (add payee, transfer, role change) proceeds on the ambient session alone | **Re-authenticate for critical actions** — require fresh reauth or dual authorization before an irreversible or high-value mutation, so a stolen session or CSRF cannot complete it | Can a critical mutation complete with only the existing session cookie? | S·w | stuttard-wahh |
| WEB-35<a name="web-35"></a> | Dynamic SQL identifier (table / column / `ORDER BY` / sort direction) built from request input | **Allowlist SQL identifiers** — a bound parameter binds a *value*, so a table, column, or sort direction taken from user input must be mapped through a fixed allowlist, or it is injection no binder can stop (↔ [[WEB-01]](security.md#web-01) identifiers are not values — binders cannot parameterize a table or column name) | Is every dynamic identifier/sort mapped through an allowlist, not concatenated? | B·w | stuttard-wahh |

## 3. PHP Security

| ID | Trigger | Rule | Answers | T·P | Src |
| --- | --- | --- | --- | --- | --- |
| PHP-01<a name="php-01"></a> | Authz/authn or token compare uses `==`/`!=` between request data and secrets/IDs | **Use strict comparison** — type juggling (`0e…` magic hashes, `"0"==false`) bypasses equality checks; prefer `===`/`hash_equals` | Are all security-sensitive compares strict (`===`/`hash_equals`)? | B·w | php-security §strict-comparison |
| PHP-02<a name="php-02"></a> | `unserialize()` called on cookies, POST bodies, cache blobs, or queue payloads | **Never unserialize untrusted data** — POP gadget chains in app/framework classes become RCE (↔ [[WEB-20]](security.md#web-20) untrusted object decode is RCE regardless of language) | Is every `unserialize` removed or limited to signed input + `allowed_classes`? | B·w | Modern PHP Security |
| PHP-03<a name="php-03"></a> | Filesystem API (`file_exists`, `file_get_contents`, `getimagesize`, …) takes a user-influenced path | **Block phar:// side-effects** — path wrappers deserialize PHAR metadata and run object chains without calling `unserialize` | Are user paths blocked from `phar://` and other dangerous wrappers? | B·w | Modern PHP Security |
| PHP-04<a name="php-04"></a> | `include`/`require` path built from request params or remote URLs | **Pin include roots** — user-controlled includes are LFI/RFI and code execution | Is the include path from a fixed map, never raw user input? | B·w | php-security §include-paths |
| PHP-05<a name="php-05"></a> | `extract($_GET)` / `extract($_POST)` / `parse_str` into the current symbol table without `EXTR_SKIP` | **Do not extract request into scope** — attackers overwrite existing variables (register_globals class) | Is `extract` on request data absent or forced to non-overwrite flags? | B·w | php-security §extract |
| PHP-06<a name="php-06"></a> | `preg_replace` with the `/e` modifier (or a dynamic pattern from user input) | **Ban preg_replace /e** — the replacement is evaluated as PHP code | Is `/e` absent and are patterns never user-controlled? | B·w | php-security §preg-replace-e |
| PHP-07<a name="php-07"></a> | Production `php.ini` leaves `exec`/`system`/`passthru`/`shell_exec`/`proc_open` enabled without need | **Disable unused dangerous functions** — post-RCE or LFI impact collapses if dangerous symbols are removed (↔ ml [[SERVE-03]](ml-systems.md#serve-03) an unused live path fires catastrophically when something else fails) | Is `disable_functions` set and verified against actual app needs? | S·r | Modern PHP Security |
| PHP-08<a name="php-08"></a> | Superglobals used raw for business decisions without `filter_input`/`filter_var` or allowlist validation | **Validate at the boundary** — untyped input reaches sinks; filter or schema-validate before use (↔ [[SEC-01]](security.md#sec-01) every trust boundary re-validates — superglobals are one crossing) | Is every external input validated to type/range before logic or sinks? | S·w | php-security §input-validation |
| PHP-09<a name="php-09"></a> | Passwords stored or checked with `md5`/`sha1`/`crypt` without a modern KDF | **Use password_hash/password_verify** — fast hashes are offline-crackable; `password_hash` uses adaptive algorithms | Are all password stores using `password_hash` (argon2/bcrypt)? | B·w | php-security §password-hash |
| PHP-10<a name="php-10"></a> | App can read paths outside its tree; `open_basedir` unset in shared/hosting PHP | **Constrain open_basedir** — LFI and path bugs cannot escape the allowed tree | Is `open_basedir` set to app + upload dirs only in prod? | S·r | Modern PHP Security |
| PHP-11<a name="php-11"></a> | Echo/`<?=` of request or DB strings into HTML without `htmlspecialchars(..., ENT_QUOTES, 'UTF-8')` or template auto-escape | **Escape every HTML emit** — PHP defaults do not encode output; XSS follows (↔ [[WEB-04]](security.md#web-04) the sink context decides the encoding, not the language default) | Is all HTML output escaped (or auto-escaped by the template engine)? | B·w | php-security §html-escape |
| PHP-12<a name="php-12"></a> | Upload handler trusts `$_FILES['type']` or the client filename extension alone | **Verify content, not client MIME** — spoofed types upload PHP polyglots; check extension allowlist + content sniff + no-exec storage | Is the extension allowlisted and the file stored non-executable outside docroot? | B·w | Modern PHP Security |
| PHP-13<a name="php-13"></a> | Class name for `new $class(...)` taken from routing/request without an allowlist | **Allowlist dynamic instantiation** — attacker-chosen classes run dangerous constructors | Is the instantiable class set a fixed allowlist? | B·w | Modern PHP Security |
| PHP-14<a name="php-14"></a> | Prod config has `display_errors=On` or broad `error_reporting` printed to clients | **Hide errors from clients** — stack traces leak paths, queries, and secrets; log instead (↔ eng [[API-05]](engineering.md#api-05) debug internals never in the client error envelope) | Are `display_errors`/`display_startup_errors` Off with `log_errors` On in prod? | S·r | Modern PHP Security |
| PHP-15<a name="php-15"></a> | SQL built with string concat of `$_GET`/`$_POST` (even via PDO `query`/`exec`) | **Bind parameters with PDO/ORM** — quote breakout becomes SQLi regardless of driver (↔ [[WEB-01]](security.md#web-01), eng [[DATA-12]](engineering.md#data-12) the same binding failure seen at the framework, web, and data layers) | Is every query using prepared statements/bound values? | B·w | Modern PHP Security |
| PHP-16<a name="php-16"></a> | Outbound HTTP/file URL from user input (`file_get_contents($url)`, curl) without a destination policy | **Constrain server-side fetches** — SSRF hits cloud metadata and internal FastCGI/admin (↔ [[WEB-07]](security.md#web-07) attacker-chosen fetch destinations reach link-local and admin nets) | Is the destination allowlisted or blocked from private/link-local ranges? | B·w | Modern PHP Security |

## 4. WordPress Security

| ID | Trigger | Rule | Answers | T·P | Src |
| --- | --- | --- | --- | --- | --- |
| WP-01<a name="wp-01"></a> | `$wpdb->query`/`get_results`/`get_var` with interpolated variables and no `$wpdb->prepare` | **Prepare every $wpdb query** — unprepared SQL is classic SQLi in plugins/themes (↔ [[WEB-01]](security.md#web-01) unparameterized SQL is injection whether in PHP, WP, or the app layer) | Is every variable SQL path using `$wpdb->prepare`? | B·w | wordpress-security §prepare |
| WP-02<a name="wp-02"></a> | Form/admin/AJAX handler that changes state without `wp_verify_nonce` / `check_admin_referer` | **Verify nonces on mutations** — missing nonces enable CSRF against logged-in users (↔ [[WEB-06]](security.md#web-06) a third party can forge the browser cookie into a state change) | Does every state-changing handler verify a nonce? | B·w | wordpress-security §nonces |
| WP-03<a name="wp-03"></a> | `add_menu_page`/`add_submenu_page` or a sensitive action runs without `current_user_can(...)` | **Gate admin with capabilities** — UI visibility is not authorization; a missing cap is privilege abuse (↔ [[WEB-31]](security.md#web-31) vertical privilege escalation) | Is `current_user_can` checked for the minimum required capability? | B·w | wordpress-security §capabilities |
| WP-04<a name="wp-04"></a> | Request data saved or used without the `sanitize_*` / `wp_kses` appropriate to the field | **Sanitize on input** — raw POST stored as options/postmeta becomes stored XSS or logic bugs later | Is every persisted field sanitized with the right `sanitize_*` helper? | S·w | wordpress-security §sanitizing |
| WP-05<a name="wp-05"></a> | Theme/plugin echoes post meta, options, or request data without `esc_html`/`esc_attr`/`esc_url`/`wp_kses_post` | **Escape on output** — WP does not auto-escape; a context-wrong `esc_*` is still XSS (↔ [[WEB-04]](security.md#web-04) escape for the HTML context on every emit, or the sink runs as the victim) | Is every echo using the correct `esc_*` for its HTML context? | B·w | wordpress-security §escaping |
| WP-06<a name="wp-06"></a> | `register_rest_route` omits `permission_callback` or uses `__return_true` on privileged routes | **Set REST permission_callback** — default-open routes expose data and mutations anonymously | Does every non-public route implement a real `permission_callback`? | B·w | wordpress-security §permission-callback |
| WP-07<a name="wp-07"></a> | XML-RPC enabled on a site that does not need remote publishing/pingbacks | **Disable unused XML-RPC** — `xmlrpc.php` enables brute-force amplification and pingback SSRF (↔ ml [[SERVE-03]](ml-systems.md#serve-03) an unused live path fires when something else fails) | Is XML-RPC off (or tightly restricted) if unused? | S·r | bootstrap |
| WP-08<a name="wp-08"></a> | Plugins/themes not updated; abandoned extensions with known CVEs still active | **Update or remove abandoned code** — unpatched plugins are the dominant WP compromise path | Are all active plugins/themes maintained and currently patched? | B·r | bootstrap |
| WP-09<a name="wp-09"></a> | `DISALLOW_FILE_EDIT` not defined true in `wp-config.php` | **Disable the file editor** — dashboard editors turn XSS or admin access into persistent RCE | Is `DISALLOW_FILE_EDIT` true in production config? | S·r | bootstrap |
| WP-10<a name="wp-10"></a> | A `wp_ajax_nopriv_*` hook performs privileged work or returns sensitive data | **Treat nopriv as public** — `nopriv` handlers are anonymous attack surface | Does every `nopriv` handler expose only intentional public behavior? | B·w | bootstrap |
| WP-11<a name="wp-11"></a> | Custom upload or `wp_handle_upload` path allows executable extensions or serves from an executable location | **Lock down media uploads** — arbitrary PHP/HTML in uploads becomes webshell or stored XSS (↔ [[WEB-12]](security.md#web-12) executable or HTML uploads outside a locked store are RCE/XSS) | Are allowed MIME/extensions strict and uploads non-executable? | B·w | bootstrap |
| WP-12<a name="wp-12"></a> | `AUTH_KEY`/`SECURE_AUTH_KEY`/`LOGGED_IN_KEY`/`NONCE_KEY` (and salts) left as defaults or duplicated | **Unique strong salts** — default or reused keys weaken cookie auth and nonce strength | Are all eight keys/salts unique, long, and not from public samples? | B·r | bootstrap |
| WP-13<a name="wp-13"></a> | Plugin/theme PHP files load application code without a `defined('ABSPATH') \|\| exit;` guard | **Guard direct file access** — direct hits bypass WP bootstrap and leak or execute partial includes | Do plugin/theme PHP entry files refuse direct access via an ABSPATH check? | S·w | bootstrap |
| WP-14<a name="wp-14"></a> | Options update / user-meta write accepts `role`, `capabilities`, or serialized objects from the request | **Block privilege mass-assignment** — user-controlled role fields create admin accounts (↔ [[WEB-19]](security.md#web-19) mass-assignment of privilege fields is an authz bypass) | Are role/capability fields excluded from user-editable forms and APIs? | B·w | bootstrap |

## 5. PostgreSQL Security

| ID | Trigger | Rule | Answers | T·P | Src |
| --- | --- | --- | --- | --- | --- |
| PG-01<a name="pg-01"></a> | Application DB role is superuser or holds cluster-wide powerful attributes (`rds_superuser`, `BYPASSRLS`) | **Run the app as least privilege** — app SQLi becomes cluster takeover if the role is superuser (↔ [[SEC-04]](security.md#sec-04) a compromised principal exercises every privilege it holds) | Is the app role non-superuser with only the schema privileges it needs? | B·r | postgresql-security §least-privilege |
| PG-02<a name="pg-02"></a> | Multi-tenant tables rely only on app `WHERE` clauses; RLS disabled or no policies | **Enable RLS policies** — a missed filter leaks rows; RLS enforces tenant isolation in the DB (↔ eng [[ARCH-13]](engineering.md#arch-13) make the structure enforce isolation so a missed filter fails closed; ↔ [[WEB-08]](security.md#web-08) object access left to handler vigilance; ↔ [[WEB-21]](security.md#web-21) a frame that steals ambient authority the surface never showed) | Is RLS ON with policies that match the tenancy model? | B·p | postgresql-security §rls |
| PG-03<a name="pg-03"></a> | `SECURITY DEFINER` function without a pinned `search_path` (or a writable schema ahead in the path) | **Pin search_path on DEFINER** — attackers plant shadow objects and hijack privileged functions | Do DEFINER functions set a fixed safe `search_path` and a trusted owner? | B·w | postgresql-security §search-path |
| PG-04<a name="pg-04"></a> | `pg_hba.conf` uses `trust`/`md5` for remote or broad CIDR matches | **Prefer scram-sha-256 auth** — `trust` is passwordless access; md5 is weaker than SCRAM | Are remote entries scram-sha-256 (not trust/md5) with tight CIDRs? | B·r | postgresql-security §auth-hba |
| PG-05<a name="pg-05"></a> | Connections allowed without SSL, or `sslmode` defaults to prefer/disable in app DSNs | **Require TLS to Postgres** — cleartext credentials and rows are MitM-visible | Is server SSL on and are clients using verify-full/verify-ca as appropriate? | B·r | postgresql-security §ssl-tls |
| PG-06<a name="pg-06"></a> | `GRANT … TO PUBLIC`, or default public-schema privileges left world-usable | **Revoke PUBLIC grants** — PUBLIC privileges give every role unintended DML/EXECUTE | Are unnecessary PUBLIC grants revoked on schemas, tables, and functions? | S·r | postgresql-security §privileges |
| PG-07<a name="pg-07"></a> | PL/pgSQL or SQL builds dynamic statements with string concat of identifiers/literals | **Use format(%I/%L) or EXECUTE … USING** — dynamic SQL at the DB layer is still SQLi (↔ [[WEB-01]](security.md#web-01) concat of identifiers/literals is injection even inside PL/pgSQL) | Is every dynamic identifier/literal passed via `%I`/`%L` or a bind param? | B·w | postgresql-security §dynamic-sql |
| PG-08<a name="pg-08"></a> | Untrusted users can `CREATE EXTENSION`, or extensions installed cluster-wide without review | **Treat extensions as code** — unpackaged C/SQL extensions expand attack surface and privilege | Are only required extensions installed, by a superuser, after review? | S·r | postgresql-security §extensions |
| PG-09<a name="pg-09"></a> | DB passwords in app repos, a world-readable `.pgpass`, or a shared superuser password across envs | **Isolate DB secrets** — a leaked password equals full data access (↔ [[WEB-16]](security.md#web-16) credentials outside a secret store are permanent credential theft) | Are DB credentials unique per env and stored only in a secret manager? | B·r | postgresql-security §secrets |
| PG-10<a name="pg-10"></a> | No base backups + WAL archiving, or an untested PITR restore path | **Prove backups restore** — untested backups fail when ransomware or a `DROP` happens | Is PITR configured and a restore drill documented as successful? | B·p | postgresql-security §backup-pitr |
| PG-11<a name="pg-11"></a> | `log_statement`/`pgaudit` off for privileged and DDL activity; no log shipping | **Log privileged SQL** — without an audit trail, abuse and SQLi exfiltration go unnoticed (↔ ux [[HAI-06]](interaction-ux.md#hai-06) without a durable log, abuse cannot be reconstructed) | Are DDL/role changes and sensitive statements logged and retained? | S·r | postgresql-security §logging |
| PG-12<a name="pg-12"></a> | `max_connections` high with no per-role/`pgbouncer` limits; app opens unbounded pools | **Cap connections** — connection storms exhaust the postmaster and cause an outage (↔ eng [[RES-14]](engineering.md#res-14) both fail when an unbounded producer exhausts a fixed resource pool) | Are server and pooler limits set below collapse thresholds per role? | S·r | postgresql-security §connections |
| PG-13<a name="pg-13"></a> | Views/functions expose rows that RLS on base tables would hide (`security_barrier`/owner ignored) | **Align views with RLS** — a non-barrier view can leak filtered rows | Do views and DEFINER paths preserve the intended row filters? | S·w | postgresql-security §security-barrier |
| PG-14<a name="pg-14"></a> | Replication or backup role holds broad write privileges beyond need | **Separate backup/repl roles** — an over-privileged ops role expands blast radius if stolen | Do backup/replication roles have only the minimal REPLICATION/read grants? | S·r | postgresql-security §replication-roles |

## 6. Secure Design Principles

> The cross-cutting engineering principles beneath every section above, from Ross Anderson, *Security Engineering*, 3rd ed. (`anderson-security-engineering`). Where a principle is the general form of a specific rule (least privilege → [[SEC-04]](security.md#sec-04)/[[PG-01]](security.md#pg-01)/[[SEC-16]](security.md#sec-16); complete mediation → [[WEB-31]](security.md#web-31)/[[WEB-33]](security.md#web-33)), the specific rule cross-links here rather than restating it. See [Principle 14].

| ID | Trigger | Rule | Answers | T·P | Src |
| --- | --- | --- | --- | --- | --- |
| SECD-01<a name="secd-01"></a> | Any system carries a protection claim ("secure", "safe", "compliant") | **Write the security policy first** — state in one paragraph what must not happen and who may do what, before choosing mechanisms; mechanism without a falsifiable policy is theatre (↔ biz [[PROD-09]](business-marketing.md#prod-09) write the channel contract before the components; ↔ a11y [[A11Y-22]](accessibility.md#a11y-22) "secure" is a falsifiable sentence or it is empty; ↔ biz [[CLM-05]](business-marketing.md#clm-05) three hostile readers who check every claim against a written spec; ↔ eng [[RLSE-09]](engineering.md#rlse-09) expert sign-off is exact quoted strings and yes/no questions, not jargon that shifts liability) | Can an engineer falsify compliance with the policy in a test? | S·p | anderson-security-engineering |
| SECD-02<a name="secd-02"></a> | A component whose compromise would break the policy | **Minimize the trusted computing base** — treat it as **trusted** and shrink that set: split duties, sandbox untrusted code, remove unneeded keys and scopes, because a fat TCB makes every bug fatal (↔ [[SEC-04]](security.md#sec-04) a jailbreak exercises every scope it holds; ↔ [[PG-01]](security.md#pg-01) app SQLi is cluster takeover if the role is superuser; ↔ [[SEC-16]](security.md#sec-16) one identity that can silently swap production weights; ↔ [[PG-06]](security.md#pg-06) PUBLIC grants hand every role unintended DML; ↔ [[PG-14]](security.md#pg-14) an over-privileged ops role expands the blast radius of a stolen credential; ↔ [Principle 14]) (↔ [[SECD-03]](security.md#secd-03) least privilege shrinks what a grant reaches; complete mediation checks every use of it, the two halves of one blast-radius discipline) | If this principal is malicious, is the whole policy dead? | B·p | anderson-security-engineering |
| SECD-03<a name="secd-03"></a> | Any security-relevant read / write / exec path | **Check every access at the reference monitor** — authorize at the reference-monitor boundary on *each* use, not only at session start (Saltzer's **complete mediation**), because check-once/use-many, CSRF, and TOCTTOU mint unearned authority (↔ [[WEB-31]](security.md#web-31), [[WEB-33]](security.md#web-33) complete mediation is deny-by-default authorization on every path) | Where is the check on *this* call, and what if the ACL changed since open? | B·w | anderson-security-engineering |
| SECD-04<a name="secd-04"></a> | An action can create or destroy value, or erase audit evidence | **Dual control for high impact** — require two independent authorizers or mechanism classes, because a single insider or single malware path is the historical failure mode (↔ [[SEC-05]](security.md#sec-05) semantic human gate) | Can one person with one laptop finish this irreversibly? | B·p | anderson-security-engineering |
| SECD-05<a name="secd-05"></a> | Installer defaults, cloud templates, API failure paths | **Fail-safe defaults** — default deny and degrade to a documented safe state rather than open privilege or network, because the default is the configuration most deployments keep (↔ [[WEB-33]](security.md#web-33) deny-by-default is the only safe installer and failure posture) | What does a naive deploy expose in five minutes? | B·w | anderson-security-engineering |
| SECD-06<a name="secd-06"></a> | A serious hazard rests on a single choke-point control | **Independent defense in depth** — require ≥2 mechanisms that fail *independently* (correlated clones don't count), because single-layer bypass is routine and fault masking hides an eroded margin | If the first layer is fully owned, what still stops the loss? | S·p | anderson-security-engineering |
| SECD-07<a name="secd-07"></a> | A new product, major redesign, or "secure AI feature" kickoff | **Threat-model before mechanism** — list assets, attacker assets, and threat-tree / STRIDE paths (including insiders and supply chain) before buying controls, because malice forces the least-convenient failure (↔ biz [[STRAT-02]](business-marketing.md#strat-02) name the ways the plan dies before the forward plan; ↔ biz [[STRAT-14]](business-marketing.md#strat-14) name the worst cases and repair paths before a reversible decision freezes) | What are the top five ways this policy dies, including insiders? | S·p | anderson-security-engineering |
| SECD-08<a name="secd-08"></a> | Humans must uphold the policy (consent, install, on-call emergency UI) | **Make the safe path the easy path** — design so the least-effort and high-stress actions are the safe ones (**psychological acceptability**), because dialog fatigue and baroque controls train bypass (↔ interaction [[COG-01]](interaction-ux.md#cog-01); human gates must stay usable under load, [[SEC-05]](security.md#sec-05)) | Under time pressure, what will a competent user actually do? | S·p | anderson-security-engineering |
| SECD-09<a name="secd-09"></a> | The incident-response plan for a template or gallery compromise lists re-enrollment, reset, or credential rotation as the remediation | **An unconcealed identifier cannot be reissued** — when a match path trusts stored body-derived identifiers (face, fingerprint, iris, voice), the compromise runbook must name a remediation the subject can actually execute: revoke the stored templates and drop that factor from the accept path until supervised re-capture, because the subject cannot be issued a new face, so "reset the credential" is a step no one can perform and the leaked template remains a valid presentation against every system holding the same person (↔ [[SECD-05]](security.md#secd-05) the failure state must be a documented safe one) | After a gallery leak, which remediation step does not require the subject to change their body? | B·p | face-recognition-compulsory-visibility |
| SECD-10<a name="secd-10"></a> | A state or authentication diagram shows biometric enrollment as the only route to a function, with no second accept path | **A biometric factor is not a sole path** — when enrollment of a body-derived identifier is required to reach a function, ship a second path of equal function (credential, token, staffed override), or record in the policy document the statutory basis and the scope it is limited to, because a matcher's false-non-match rate is not uniform across cohorts (↔ ml [[FAIR-03]](ml-systems.md#fair-03), [[FAIR-04]](ml-systems.md#fair-04) name the per-cohort error at the production threshold; this row is what that error becomes when the path is sole — an exclusion rather than a retry) so a sole biometric path converts a per-cohort error rate into a per-cohort exclusion with no recovery, and subjects who cannot enroll at all — injury, capture failure, refusal — have no route (↔ [[SECD-08]](security.md#secd-08) the safe path must also be a reachable one) | Can a user finish this task without enrolling a biometric, and by what path? | S·p | face-recognition-compulsory-visibility |

## 7. Cross-lexicon links

Security is a spanning concern, so many rows restate a rule another lexicon owns from the security angle:

- **Injection is one mechanism across three sinks.** SQL parameterization [[WEB-01]](security.md#web-01)/[[PHP-15]](security.md#php-15)/[[WP-01]](security.md#wp-01)/[[PG-07]](security.md#pg-07) is engineering's [[DATA-12]](engineering.md#data-12) read as an attack; the LLM analogue is [[SEC-02]](security.md#sec-02) (treat model output as untrusted before any interpreter).
- **Least privilege repeats up the stack.** [[PG-01]](security.md#pg-01) (non-superuser app role) and [[SEC-04]](security.md#sec-04) (least-privilege agency) are the same rule at the database and the model; both bound the blast radius of a compromised component (↔ eng [[ARCH-02]](engineering.md#arch-02), single writer owns the table).
- **Secrets discipline is one rule.** [[WEB-16]](security.md#web-16)/[[PG-09]](security.md#pg-09) and [[SEC-06]](security.md#sec-06) all say the same thing: anything a component can read, an attacker who reaches that component can read too.
- **Rate limiting is denial-of-wallet and DoS at once.** [[WEB-17]](security.md#web-17) and [[SEC-08]](security.md#sec-08) share a source mechanism; unmetered access is both an availability and a cost failure.
- **Validation at the trust boundary** ([[SEC-01]](security.md#sec-01), [[PHP-08]](security.md#php-08)) is the security face of the same boundary the engineering lexicon guards for correctness ([[API-11]](engineering.md#api-11) round-trip, [[DATA-03]](engineering.md#data-03) schema compat).

## Consumption

Canonical source. Consuming repos sync this file and cite rules by ID (`[WEB-01]`, `[SEC-04]`). The `§1` LLM rules moved here from engineering keep their `SEC-*` IDs. Because §§2–5 are a bootstrap pass, a consumer wiring `bootstrap` rows into a gate should treat them as strong review defaults, not cited Blockers, until the grounding pass lands. Product-specific security targets (which controls, which environments, by when) live in the consuming project, not here.

<!-- BEGIN GENERATED SOURCES -->

## Sources

Every `Src` cell above names one of these. The citation is the work itself; the distillation behind it stays in the private research corpus.

| Src | Source | Rules here |
|---|---|---|
| `anderson-security-engineering` | Ross Anderson, *Security Engineering*, 3rd ed., Wiley 2020 (ch. 1 What Is Security Engineering; ch. 6 Access Control; ch. 27 Secure Systems Development). Primary source | 8 |
| `bootstrap` | bootstrap | 8 |
| `face-recognition-compulsory-visibility` | Ian Berle, *Face Recognition Technology: Compulsory Visibility and Its Impact on Privacy and the Confidentiality of Personal Identifiable Information*, Springer Law, Governance and Technology Series 41 (2020). Primary source. [ISBN 978-3-030-36887-6 | 2 |
| `handbook-face-recognition` | Stan Z. Li, Anil K. Jain, Jiankang Deng (eds.), *Handbook of Face Recognition: The Deep Neural Network Approach*, 3rd ed., Springer, 2024. Primary source. [ISBN / DOI via Springer; chapter DOIs 10.1007/978-3-031-43567-6_*] | 4 |
| `llm-security-playbook` | Steve Wilson, *The Developer's Playbook for Large Language Model Security: Building Secure AI Applications*, 1st ed. 2024 (O'Reilly) | 10 |
| `modern-php-security` | Modern PHP Security | 9 |
| `owasp-web-hardening` | OWASP Cheat Sheet Series — *File Upload Cheat Sheet*, *Mass Assignment Cheat Sheet*, *Deserialization Cheat Sheet*, *Secrets Management Cheat Sheet*. Published by the Open Web Application Security Project (OWASP). Primary documentation (living cheat sheets, not a fixed book edition) | 4 |
| `php-security` | PHP Manual (PHP Documentation Group) and OWASP Cheat Sheet Series — | 7 |
| `postgresql-security` | PostgreSQL Global Development Group, *PostgreSQL 18 Documentation* (current as of fetch), platform security pages listed below | 14 |
| `sotiropoulos-adversarial-ai` | John Sotiropoulos, *Adversarial AI Attacks, Mitigations, and Defense Strategies*, Packt 2024. Primary source (selected: Ch. 4 Poisoning; Ch. 5 Trojan/model reprogramming; Ch. 6 Supply chain; Ch. 7 Evasion; Part 3 front-matter only for privacy chapters) | 6 |
| `stuttard-wahh` | Stuttard & Pinto, *The Web Application Hacker’s Handbook*, 2nd ed., Wiley 2011. Primary source | 19 |
| `wordpress-security` | WordPress Developer Resources (developer.wordpress.org), *Plugin/Theme Security handbooks and Code Reference*, published by the WordPress Documentation / Developer Relations community under the WordPress Foundation umbrella. Primary platform documentation (not a book). Fetched **2026-07-20** from: | 6 |
| `zalewski-tangled-web` | Michal Zalewski, *The Tangled Web: A Guide to Securing Modern Web Applications*, No Starch Press 2011. Primary source | 12 |

<!-- END GENERATED SOURCES -->
